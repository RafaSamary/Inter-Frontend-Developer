
// COLEÇÃO MAP

const myMaps = new Map()


myMaps.set('Nome', 'Rafael'); // passa por parametro chave e valor

myMaps.get("Nome"); // retorna o valor da chave

myMaps.delete("Nome") // deleta a chave e valor


/* Maps diferente dos Objetos eles podem ser qualquer tipo,
   possuem a propriedade length
   valores tem o mesmo tipo*/ 


   // COLEÇÃO SET 


   const mySets = new set();

    mySets.add(1); // adicionar valor

    mySets.has(1) // retornar valor

    mySets.delete(1) // deletar valor


    /* A diferença em SET e ARRAY, 
      possui valores unicos,
      propriedade SIZE e não LENGTH,
      não possui metodos MAP, FILTER,REDUCE etc... */ 


